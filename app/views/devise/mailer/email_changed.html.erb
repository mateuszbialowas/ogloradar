<%= render Mailers::BaseComponent.new do |component| %>
  <% component.with_header do %>
    Cześć <%= @email %> 👋!
  <% end %>
  <%= component.with_body do %>
    <p style="margin: 0; line-height: 24px">
      <% if @resource.try(:unconfirmed_email?) %>

      <p>Kontaktujemy się z Tobą, aby poinformować, że Twój adres e-mail zostaje zmieniony
        na <%= @resource.unconfirmed_email %>.</p>
    <% else %>
      <p>Kontaktujemy się z Tobą, aby poinformować, że Twój adres e-mail został zmieniony na <%= @resource.email %>.</p>
    <% end %>    </p>
    <div>
    </div>
  <% end %>
<% end %>
